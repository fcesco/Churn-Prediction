import pandas as pd

class graphModel(object):
    def __init__(self, nodes, edges, y, threshold1=0.7, threshold2= 0.4, neighbors=1):
        self.nodes = nodes
        self.edges = edges
        self.threshold1 = threshold1
        self.threshold2 = threshold2

    def optimize_model(self):


    def score_model(self):
